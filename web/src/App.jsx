import '@/App.css';

export default function App({}) {
  // Here, you will build the UI

  return (
    <div>
      <code>Any content we want to display in NUI. Should be defined here</code>
    </div>
  );
}
